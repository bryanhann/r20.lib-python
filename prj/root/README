To initialize execute the following:

./.init
source ./.activate
pip install --upgrade pip
pip install -r ./config/pip.freeze

Then you may try commands like

python3 -m bh -h
python3 -m demo.A.B.C --dump


